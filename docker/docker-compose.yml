services:

  postgres:
    image: postgres:17

    container_name: posto-db

    env_file:
      - ../.env

    ports:
      - "5432:5432"

    volumes:
      - ../database/DDLs:/docker-entrypoint-initdb.d

    restart: unless-stopped